server:
  port: 8080

aws:
  defaultSimpleDBDomain: CLOUD_REGISTRY
  simpleDBEnabled: false

spinnaker:
  cassandra:
    enabled: true
    cluster: CASS_SPINNAKER
    keyspace: front50
    name: default

cassandra:
  embedded: true
  host: 127.0.0.1 

logging:
  level:
    retrofit: DEBUG
